.DS_Store
.vscode/
src/version.h
__pycache__/
*.pyc

build
build-*
html
CMakeFiles

# example/ build artifacts: every example ships its source structure and its
# .msp/.vmd/Makefile, and generates the rest with `make prep` (`make clean`
# removes them again).
example/*/*.nc
example/*/*.nc.pdb
#076 concatenates its parameter files at build time (see its Makefile)
example/*/complex.grp
example/*/complex.nbi.ff
example/*/complex.rbody
example/*/complex.bi.ff
example/*/mddriver.log